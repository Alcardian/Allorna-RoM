#ifndef ALCTOOLS_HPP
#define ALCTOOLS_HPP

#include <string>

using namespace std;

class alctools
{
      public:
             bool isnumber(string number);
             int stringtonumber(string number);
};
#endif
